name: atmos_dashboard
description: A high-contrast, brutalist-style air quality dashboard.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  geolocator: ^10.1.0
  intl: ^0.18.1
  fl_chart: ^0.65.0
  url_launcher: ^6.2.0
  provider: ^6.0.0
  google_generative_ai: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
